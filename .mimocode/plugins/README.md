# MiMoCode Plugins

No MiMoCode plugins are enabled by default in this initial adapter release.
Add plugins only after they are represented in `config/projection-policy.json`
and validated as reviewable source changes.
