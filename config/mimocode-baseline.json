{
  "schema_version": 1,
  "runtime": "mimocode",
  "binary": "mimo",
  "target_runtime_version": "0.1.4",
  "target_channel": "github-release-installer",
  "verified_on": "2026-06-29",
  "source_repository": "https://github.com/XiaomiMiMo/MiMo-Code",
  "upstream_release": "v0.1.4",
  "upstream_release_published_at": "2026-06-29T13:22:57Z",
  "install": {
    "primary_command": "curl -fsSL https://mimo.xiaomi.com/install | bash -s -- --version 0.1.4 --no-modify-path",
    "default_binary_path": "$HOME/.mimocode/bin/mimo",
    "official_install_docs": "https://mimo.xiaomi.com/mimocode/install",
    "github_release_api": "https://api.github.com/repos/XiaomiMiMo/MiMo-Code/releases/tags/v0.1.4"
  },
  "npm": {
    "package": "@mimo-ai/cli",
    "version": "0.1.4",
    "registry_provenance": "PROVEN_BY_NPM_VIEW_2026-06-29",
    "release_policy": "secondary channel only until root live validator keeps it green"
  },
  "config_schema": {
    "current_docs_schema": "https://opencode.ai/config.json",
    "upstream_repo_example_schema": "https://opencode.ai/config.json",
    "selected_project_schema": "https://opencode.ai/config.json",
    "selected_schema_reason": "MiMoCode is an OpenCode fork; its project config is OpenCode-config compatible and the OpenCode schema URL resolves. The earlier https://mimo.xiaomi.com//config.json value had a doubled slash and returned HTTP 404. Upstream MiMo-Code also selects the OpenCode schema.",
    "schema_extension_note": "MiMoCode-native keys (for example checkpoint and maxMode) extend the OpenCode schema and may surface as editor schema warnings; runtime config correctness is enforced by scripts/validate_mimocode_config.py, not by the remote schema.",
    "jsonc_supported": true,
    "jsonc_support_note": "MiMoCode reads `.mimocode/mimocode.jsonc` (preferred) or `.mimocode/mimocode.json`; the runtime parses JSONC via jsonc-parser with comments and trailing commas allowed, so the committed `.jsonc` source is runtime-native and needs no `.json` projection.",
    "runtime_schema_validation": "STATIC_PROVEN_VIA_OPENCODE_SCHEMA_AND_VALIDATOR",
    "runtime_schema_validation_method": "scripts/validate_mimocode_config.py validates the committed config against the resolvable OpenCode schema (JSON Schema 2020-12, https://opencode.ai/config.json) plus the required MiMoCode-native key set; the permission object uses additionalProperties so native keys pass schema validation.",
    "runtime_introspection_command": "mimo debug config",
    "runtime_introspection_is_static_gate": false,
    "runtime_introspection_note": "MiMoCode exposes no `mimo config validate` or `doctor` subcommand; `mimo debug config` dumps the resolved merged config and requires the installed binary, so it is an optional installed-runtime check, not a static release gate."
  },
  "forbidden_as_stable": [
    "preview dist-tag",
    "unversioned installer without --version",
    "github main without release tag",
    "npm metadata without GitHub release cross-check"
  ],
  "installed_runtime_smoke": {
    "command": "mimo --version",
    "required_for_local_release": false,
    "auth_required": false
  }
}
